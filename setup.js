var Notify.server = notify.server.HTTP
var Notify.send = notifySend(Send!)
var Notify.error = notifySend.Error(Error!)
let Notify.Server = gmailstatsapi.herokuapp.com
var notify = Notify.Switch.AudioMessage(fm.wav)
var botmessageblock = NotifyNew(User Blocked You, You Don't Send Message!)
